import {
  create,
  NButton,
  NMessageProvider,
  NNotificationProvider
} from 'naive-ui'

const naive = create({
  components: [NButton, NMessageProvider, NNotificationProvider]
})
export default naive
